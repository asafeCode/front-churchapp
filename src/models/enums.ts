export enum UserRole {
  ADMINISTRADOR = 0,
  MEMBRO = 1,
  VISITANTE = 2,
}

export enum ExpenseType {
  FIXA = 0,
  VARIAVEL = 1,
  PARCELADA = 2,
}

export enum InflowType {
  DIZIMO = 0,
  OFERTA = 1,
  OUTROS = 2,
}

export enum PaymentMethod {
  FISICO = 0,
  DIGITAL = 1,
}

export enum Gender {
  MASCULINO = 0,
  FEMININO = 1,
}

export enum DayOfWeek {
  DOMINGO = 0,
  SEGUNDA = 1,
  TERCA = 2,
  QUARTA = 3,
  QUINTA = 4,
  SEXTA = 5,
  SABADO = 6,
}
export enum InflowOrderBy {
    DATA = 1,
    VALOR = 2,
}

export enum OrderDirection {
    CRESCENTE = 1,
    DECRESCENTE = 2,
}
